import { RegistrationBookInterface } from '../../../../share/interface/registration-book.interface copy'

type TabRegistrationBookProps = {
  data?: RegistrationBookInterface
}

const RegistrationBook = ({ data }: TabRegistrationBookProps): JSX.Element => {
  return <></>
}
export default RegistrationBook
